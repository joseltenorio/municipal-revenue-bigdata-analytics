-- Silver external tables
-- Generated from existing Parquet datasets.
-- Do not edit data files from Hive; these are external lakehouse tables.

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`municipal_classification__classification_2019` (
  `ubigeo6` STRING,
  `tipo_clasificacion_municipal` STRING,
  `ambito_municipal` STRING,
  `descripcion_tipo` STRING,
  `departamento_nombre` STRING,
  `provincia_nombre` STRING,
  `distrito_nombre` STRING,
  `source_row_number` INT,
  `is_valid_ubigeo6` BOOLEAN,
  `is_valid_tipo_clasificacion_municipal` BOOLEAN,
  `is_valid_ambito_municipal` BOOLEAN,
  `is_valid_tipo_ambito_consistency` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/municipal_classification/resource_key=classification_2019';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`renamu__municipal_context` (
  `anio` INT,
  `idmunici` STRING,
  `ccdd` STRING,
  `ccpp` STRING,
  `ccdi` STRING,
  `ubigeo6` STRING,
  `departamento_nombre` STRING,
  `provincia_nombre` STRING,
  `distrito_nombre` STRING,
  `tipomuni_codigo` STRING,
  `tipomuni_nombre` STRING,
  `total_computadoras_operativas` INT,
  `cuenta_servicio_internet` BOOLEAN,
  `computadoras_con_acceso_internet` INT,
  `tipo_conexion_internet_codigo` STRING,
  `tipo_conexion_internet_nombre` STRING,
  `usa_siaf` BOOLEAN,
  `usa_sistema_recaudacion_tributaria_municipal` BOOLEAN,
  `usa_sistema_rentas_administracion_tributaria` BOOLEAN,
  `usa_sistema_catastro` BOOLEAN,
  `no_tiene_sistemas_gestion` BOOLEAN,
  `portal_transparencia_estado_codigo` STRING,
  `portal_transparencia_estado_nombre` STRING,
  `tiene_portal_transparencia` BOOLEAN,
  `portal_transparencia_actualizado` BOOLEAN,
  `portal_transparencia_url` STRING,
  `total_personal_dic_2021` INT,
  `total_personal_mar_2022` INT,
  `tiene_personal_locacion_servicios` BOOLEAN,
  `personal_locacion_total_dic_2021` INT,
  `personal_locacion_total_mar_2022` INT,
  `tiene_personal_discapacidad` BOOLEAN,
  `personal_discapacidad_total_dic_2021` INT,
  `personal_discapacidad_total_mar_2022` INT,
  `acepta_pago_efectivo_ventanilla` BOOLEAN,
  `acepta_pago_tarjeta_ventanilla` BOOLEAN,
  `acepta_pago_web_en_linea` BOOLEAN,
  `acepta_otro_medio_pago` BOOLEAN,
  `tiene_personal_exclusivo_administracion_tributaria` BOOLEAN,
  `personal_admin_tributaria_dic_2021` INT,
  `personal_admin_tributaria_mar_2022` INT,
  `tiene_area_ejecucion_coactiva` BOOLEAN,
  `requiere_asistencia_administracion_tributaria` BOOLEAN,
  `requiere_asistencia_catastro` BOOLEAN,
  `requiere_capacitacion_administracion_tributaria` BOOLEAN,
  `requiere_capacitacion_catastro` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/renamu/resource_key=municipal_context';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`siaf_income__annual_2012` (
  `anio` INT,
  `mes` INT,
  `fecha_mes` DATE,
  `source_resource_key` STRING,
  `source_granularity` STRING,
  `nivel_gobierno_codigo` STRING,
  `nivel_gobierno_nombre` STRING,
  `sector_codigo` STRING,
  `sector_nombre` STRING,
  `pliego_codigo` STRING,
  `pliego_nombre` STRING,
  `sec_ejec` STRING,
  `ejecutora_codigo` STRING,
  `ejecutora_nombre` STRING,
  `departamento_codigo` STRING,
  `departamento_nombre` STRING,
  `provincia_codigo` STRING,
  `provincia_nombre` STRING,
  `distrito_codigo` STRING,
  `distrito_nombre` STRING,
  `ubigeo6_ejecutora` STRING,
  `fuente_financiamiento_codigo` STRING,
  `fuente_financiamiento_nombre` STRING,
  `rubro_codigo` STRING,
  `rubro_nombre` STRING,
  `tipo_recurso_codigo` STRING,
  `tipo_recurso_nombre` STRING,
  `generica_codigo` STRING,
  `generica_nombre` STRING,
  `subgenerica_codigo` STRING,
  `subgenerica_nombre` STRING,
  `subgenerica_det_codigo` STRING,
  `subgenerica_det_nombre` STRING,
  `especifica_codigo` STRING,
  `especifica_nombre` STRING,
  `especifica_det_codigo` STRING,
  `especifica_det_nombre` STRING,
  `monto_pia` DECIMAL(20,4),
  `monto_pim` DECIMAL(20,4),
  `monto_recaudado` DECIMAL(20,4),
  `is_municipal_government` BOOLEAN,
  `is_valid_anio` BOOLEAN,
  `is_valid_mes` BOOLEAN,
  `is_valid_sec_ejec` BOOLEAN,
  `is_valid_ubigeo6_ejecutora` BOOLEAN,
  `is_valid_monto_pia` BOOLEAN,
  `is_valid_monto_pim` BOOLEAN,
  `is_valid_monto_recaudado` BOOLEAN,
  `flag_pim_menor_pia` BOOLEAN,
  `flag_recaudado_mayor_pim` BOOLEAN,
  `has_complete_territory` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/siaf_income/resource_key=annual_2012';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`siaf_income__annual_2013` (
  `anio` INT,
  `mes` INT,
  `fecha_mes` DATE,
  `source_resource_key` STRING,
  `source_granularity` STRING,
  `nivel_gobierno_codigo` STRING,
  `nivel_gobierno_nombre` STRING,
  `sector_codigo` STRING,
  `sector_nombre` STRING,
  `pliego_codigo` STRING,
  `pliego_nombre` STRING,
  `sec_ejec` STRING,
  `ejecutora_codigo` STRING,
  `ejecutora_nombre` STRING,
  `departamento_codigo` STRING,
  `departamento_nombre` STRING,
  `provincia_codigo` STRING,
  `provincia_nombre` STRING,
  `distrito_codigo` STRING,
  `distrito_nombre` STRING,
  `ubigeo6_ejecutora` STRING,
  `fuente_financiamiento_codigo` STRING,
  `fuente_financiamiento_nombre` STRING,
  `rubro_codigo` STRING,
  `rubro_nombre` STRING,
  `tipo_recurso_codigo` STRING,
  `tipo_recurso_nombre` STRING,
  `generica_codigo` STRING,
  `generica_nombre` STRING,
  `subgenerica_codigo` STRING,
  `subgenerica_nombre` STRING,
  `subgenerica_det_codigo` STRING,
  `subgenerica_det_nombre` STRING,
  `especifica_codigo` STRING,
  `especifica_nombre` STRING,
  `especifica_det_codigo` STRING,
  `especifica_det_nombre` STRING,
  `monto_pia` DECIMAL(20,4),
  `monto_pim` DECIMAL(20,4),
  `monto_recaudado` DECIMAL(20,4),
  `is_municipal_government` BOOLEAN,
  `is_valid_anio` BOOLEAN,
  `is_valid_mes` BOOLEAN,
  `is_valid_sec_ejec` BOOLEAN,
  `is_valid_ubigeo6_ejecutora` BOOLEAN,
  `is_valid_monto_pia` BOOLEAN,
  `is_valid_monto_pim` BOOLEAN,
  `is_valid_monto_recaudado` BOOLEAN,
  `flag_pim_menor_pia` BOOLEAN,
  `flag_recaudado_mayor_pim` BOOLEAN,
  `has_complete_territory` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/siaf_income/resource_key=annual_2013';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`siaf_income__annual_2014` (
  `anio` INT,
  `mes` INT,
  `fecha_mes` DATE,
  `source_resource_key` STRING,
  `source_granularity` STRING,
  `nivel_gobierno_codigo` STRING,
  `nivel_gobierno_nombre` STRING,
  `sector_codigo` STRING,
  `sector_nombre` STRING,
  `pliego_codigo` STRING,
  `pliego_nombre` STRING,
  `sec_ejec` STRING,
  `ejecutora_codigo` STRING,
  `ejecutora_nombre` STRING,
  `departamento_codigo` STRING,
  `departamento_nombre` STRING,
  `provincia_codigo` STRING,
  `provincia_nombre` STRING,
  `distrito_codigo` STRING,
  `distrito_nombre` STRING,
  `ubigeo6_ejecutora` STRING,
  `fuente_financiamiento_codigo` STRING,
  `fuente_financiamiento_nombre` STRING,
  `rubro_codigo` STRING,
  `rubro_nombre` STRING,
  `tipo_recurso_codigo` STRING,
  `tipo_recurso_nombre` STRING,
  `generica_codigo` STRING,
  `generica_nombre` STRING,
  `subgenerica_codigo` STRING,
  `subgenerica_nombre` STRING,
  `subgenerica_det_codigo` STRING,
  `subgenerica_det_nombre` STRING,
  `especifica_codigo` STRING,
  `especifica_nombre` STRING,
  `especifica_det_codigo` STRING,
  `especifica_det_nombre` STRING,
  `monto_pia` DECIMAL(20,4),
  `monto_pim` DECIMAL(20,4),
  `monto_recaudado` DECIMAL(20,4),
  `is_municipal_government` BOOLEAN,
  `is_valid_anio` BOOLEAN,
  `is_valid_mes` BOOLEAN,
  `is_valid_sec_ejec` BOOLEAN,
  `is_valid_ubigeo6_ejecutora` BOOLEAN,
  `is_valid_monto_pia` BOOLEAN,
  `is_valid_monto_pim` BOOLEAN,
  `is_valid_monto_recaudado` BOOLEAN,
  `flag_pim_menor_pia` BOOLEAN,
  `flag_recaudado_mayor_pim` BOOLEAN,
  `has_complete_territory` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/siaf_income/resource_key=annual_2014';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`siaf_income__annual_2015` (
  `anio` INT,
  `mes` INT,
  `fecha_mes` DATE,
  `source_resource_key` STRING,
  `source_granularity` STRING,
  `nivel_gobierno_codigo` STRING,
  `nivel_gobierno_nombre` STRING,
  `sector_codigo` STRING,
  `sector_nombre` STRING,
  `pliego_codigo` STRING,
  `pliego_nombre` STRING,
  `sec_ejec` STRING,
  `ejecutora_codigo` STRING,
  `ejecutora_nombre` STRING,
  `departamento_codigo` STRING,
  `departamento_nombre` STRING,
  `provincia_codigo` STRING,
  `provincia_nombre` STRING,
  `distrito_codigo` STRING,
  `distrito_nombre` STRING,
  `ubigeo6_ejecutora` STRING,
  `fuente_financiamiento_codigo` STRING,
  `fuente_financiamiento_nombre` STRING,
  `rubro_codigo` STRING,
  `rubro_nombre` STRING,
  `tipo_recurso_codigo` STRING,
  `tipo_recurso_nombre` STRING,
  `generica_codigo` STRING,
  `generica_nombre` STRING,
  `subgenerica_codigo` STRING,
  `subgenerica_nombre` STRING,
  `subgenerica_det_codigo` STRING,
  `subgenerica_det_nombre` STRING,
  `especifica_codigo` STRING,
  `especifica_nombre` STRING,
  `especifica_det_codigo` STRING,
  `especifica_det_nombre` STRING,
  `monto_pia` DECIMAL(20,4),
  `monto_pim` DECIMAL(20,4),
  `monto_recaudado` DECIMAL(20,4),
  `is_municipal_government` BOOLEAN,
  `is_valid_anio` BOOLEAN,
  `is_valid_mes` BOOLEAN,
  `is_valid_sec_ejec` BOOLEAN,
  `is_valid_ubigeo6_ejecutora` BOOLEAN,
  `is_valid_monto_pia` BOOLEAN,
  `is_valid_monto_pim` BOOLEAN,
  `is_valid_monto_recaudado` BOOLEAN,
  `flag_pim_menor_pia` BOOLEAN,
  `flag_recaudado_mayor_pim` BOOLEAN,
  `has_complete_territory` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/siaf_income/resource_key=annual_2015';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`siaf_income__annual_2016` (
  `anio` INT,
  `mes` INT,
  `fecha_mes` DATE,
  `source_resource_key` STRING,
  `source_granularity` STRING,
  `nivel_gobierno_codigo` STRING,
  `nivel_gobierno_nombre` STRING,
  `sector_codigo` STRING,
  `sector_nombre` STRING,
  `pliego_codigo` STRING,
  `pliego_nombre` STRING,
  `sec_ejec` STRING,
  `ejecutora_codigo` STRING,
  `ejecutora_nombre` STRING,
  `departamento_codigo` STRING,
  `departamento_nombre` STRING,
  `provincia_codigo` STRING,
  `provincia_nombre` STRING,
  `distrito_codigo` STRING,
  `distrito_nombre` STRING,
  `ubigeo6_ejecutora` STRING,
  `fuente_financiamiento_codigo` STRING,
  `fuente_financiamiento_nombre` STRING,
  `rubro_codigo` STRING,
  `rubro_nombre` STRING,
  `tipo_recurso_codigo` STRING,
  `tipo_recurso_nombre` STRING,
  `generica_codigo` STRING,
  `generica_nombre` STRING,
  `subgenerica_codigo` STRING,
  `subgenerica_nombre` STRING,
  `subgenerica_det_codigo` STRING,
  `subgenerica_det_nombre` STRING,
  `especifica_codigo` STRING,
  `especifica_nombre` STRING,
  `especifica_det_codigo` STRING,
  `especifica_det_nombre` STRING,
  `monto_pia` DECIMAL(20,4),
  `monto_pim` DECIMAL(20,4),
  `monto_recaudado` DECIMAL(20,4),
  `is_municipal_government` BOOLEAN,
  `is_valid_anio` BOOLEAN,
  `is_valid_mes` BOOLEAN,
  `is_valid_sec_ejec` BOOLEAN,
  `is_valid_ubigeo6_ejecutora` BOOLEAN,
  `is_valid_monto_pia` BOOLEAN,
  `is_valid_monto_pim` BOOLEAN,
  `is_valid_monto_recaudado` BOOLEAN,
  `flag_pim_menor_pia` BOOLEAN,
  `flag_recaudado_mayor_pim` BOOLEAN,
  `has_complete_territory` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/siaf_income/resource_key=annual_2016';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`siaf_income__annual_2017` (
  `anio` INT,
  `mes` INT,
  `fecha_mes` DATE,
  `source_resource_key` STRING,
  `source_granularity` STRING,
  `nivel_gobierno_codigo` STRING,
  `nivel_gobierno_nombre` STRING,
  `sector_codigo` STRING,
  `sector_nombre` STRING,
  `pliego_codigo` STRING,
  `pliego_nombre` STRING,
  `sec_ejec` STRING,
  `ejecutora_codigo` STRING,
  `ejecutora_nombre` STRING,
  `departamento_codigo` STRING,
  `departamento_nombre` STRING,
  `provincia_codigo` STRING,
  `provincia_nombre` STRING,
  `distrito_codigo` STRING,
  `distrito_nombre` STRING,
  `ubigeo6_ejecutora` STRING,
  `fuente_financiamiento_codigo` STRING,
  `fuente_financiamiento_nombre` STRING,
  `rubro_codigo` STRING,
  `rubro_nombre` STRING,
  `tipo_recurso_codigo` STRING,
  `tipo_recurso_nombre` STRING,
  `generica_codigo` STRING,
  `generica_nombre` STRING,
  `subgenerica_codigo` STRING,
  `subgenerica_nombre` STRING,
  `subgenerica_det_codigo` STRING,
  `subgenerica_det_nombre` STRING,
  `especifica_codigo` STRING,
  `especifica_nombre` STRING,
  `especifica_det_codigo` STRING,
  `especifica_det_nombre` STRING,
  `monto_pia` DECIMAL(20,4),
  `monto_pim` DECIMAL(20,4),
  `monto_recaudado` DECIMAL(20,4),
  `is_municipal_government` BOOLEAN,
  `is_valid_anio` BOOLEAN,
  `is_valid_mes` BOOLEAN,
  `is_valid_sec_ejec` BOOLEAN,
  `is_valid_ubigeo6_ejecutora` BOOLEAN,
  `is_valid_monto_pia` BOOLEAN,
  `is_valid_monto_pim` BOOLEAN,
  `is_valid_monto_recaudado` BOOLEAN,
  `flag_pim_menor_pia` BOOLEAN,
  `flag_recaudado_mayor_pim` BOOLEAN,
  `has_complete_territory` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/siaf_income/resource_key=annual_2017';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`siaf_income__annual_2018` (
  `anio` INT,
  `mes` INT,
  `fecha_mes` DATE,
  `source_resource_key` STRING,
  `source_granularity` STRING,
  `nivel_gobierno_codigo` STRING,
  `nivel_gobierno_nombre` STRING,
  `sector_codigo` STRING,
  `sector_nombre` STRING,
  `pliego_codigo` STRING,
  `pliego_nombre` STRING,
  `sec_ejec` STRING,
  `ejecutora_codigo` STRING,
  `ejecutora_nombre` STRING,
  `departamento_codigo` STRING,
  `departamento_nombre` STRING,
  `provincia_codigo` STRING,
  `provincia_nombre` STRING,
  `distrito_codigo` STRING,
  `distrito_nombre` STRING,
  `ubigeo6_ejecutora` STRING,
  `fuente_financiamiento_codigo` STRING,
  `fuente_financiamiento_nombre` STRING,
  `rubro_codigo` STRING,
  `rubro_nombre` STRING,
  `tipo_recurso_codigo` STRING,
  `tipo_recurso_nombre` STRING,
  `generica_codigo` STRING,
  `generica_nombre` STRING,
  `subgenerica_codigo` STRING,
  `subgenerica_nombre` STRING,
  `subgenerica_det_codigo` STRING,
  `subgenerica_det_nombre` STRING,
  `especifica_codigo` STRING,
  `especifica_nombre` STRING,
  `especifica_det_codigo` STRING,
  `especifica_det_nombre` STRING,
  `monto_pia` DECIMAL(20,4),
  `monto_pim` DECIMAL(20,4),
  `monto_recaudado` DECIMAL(20,4),
  `is_municipal_government` BOOLEAN,
  `is_valid_anio` BOOLEAN,
  `is_valid_mes` BOOLEAN,
  `is_valid_sec_ejec` BOOLEAN,
  `is_valid_ubigeo6_ejecutora` BOOLEAN,
  `is_valid_monto_pia` BOOLEAN,
  `is_valid_monto_pim` BOOLEAN,
  `is_valid_monto_recaudado` BOOLEAN,
  `flag_pim_menor_pia` BOOLEAN,
  `flag_recaudado_mayor_pim` BOOLEAN,
  `has_complete_territory` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/siaf_income/resource_key=annual_2018';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`siaf_income__annual_2019` (
  `anio` INT,
  `mes` INT,
  `fecha_mes` DATE,
  `source_resource_key` STRING,
  `source_granularity` STRING,
  `nivel_gobierno_codigo` STRING,
  `nivel_gobierno_nombre` STRING,
  `sector_codigo` STRING,
  `sector_nombre` STRING,
  `pliego_codigo` STRING,
  `pliego_nombre` STRING,
  `sec_ejec` STRING,
  `ejecutora_codigo` STRING,
  `ejecutora_nombre` STRING,
  `departamento_codigo` STRING,
  `departamento_nombre` STRING,
  `provincia_codigo` STRING,
  `provincia_nombre` STRING,
  `distrito_codigo` STRING,
  `distrito_nombre` STRING,
  `ubigeo6_ejecutora` STRING,
  `fuente_financiamiento_codigo` STRING,
  `fuente_financiamiento_nombre` STRING,
  `rubro_codigo` STRING,
  `rubro_nombre` STRING,
  `tipo_recurso_codigo` STRING,
  `tipo_recurso_nombre` STRING,
  `generica_codigo` STRING,
  `generica_nombre` STRING,
  `subgenerica_codigo` STRING,
  `subgenerica_nombre` STRING,
  `subgenerica_det_codigo` STRING,
  `subgenerica_det_nombre` STRING,
  `especifica_codigo` STRING,
  `especifica_nombre` STRING,
  `especifica_det_codigo` STRING,
  `especifica_det_nombre` STRING,
  `monto_pia` DECIMAL(20,4),
  `monto_pim` DECIMAL(20,4),
  `monto_recaudado` DECIMAL(20,4),
  `is_municipal_government` BOOLEAN,
  `is_valid_anio` BOOLEAN,
  `is_valid_mes` BOOLEAN,
  `is_valid_sec_ejec` BOOLEAN,
  `is_valid_ubigeo6_ejecutora` BOOLEAN,
  `is_valid_monto_pia` BOOLEAN,
  `is_valid_monto_pim` BOOLEAN,
  `is_valid_monto_recaudado` BOOLEAN,
  `flag_pim_menor_pia` BOOLEAN,
  `flag_recaudado_mayor_pim` BOOLEAN,
  `has_complete_territory` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/siaf_income/resource_key=annual_2019';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`siaf_income__annual_2020` (
  `anio` INT,
  `mes` INT,
  `fecha_mes` DATE,
  `source_resource_key` STRING,
  `source_granularity` STRING,
  `nivel_gobierno_codigo` STRING,
  `nivel_gobierno_nombre` STRING,
  `sector_codigo` STRING,
  `sector_nombre` STRING,
  `pliego_codigo` STRING,
  `pliego_nombre` STRING,
  `sec_ejec` STRING,
  `ejecutora_codigo` STRING,
  `ejecutora_nombre` STRING,
  `departamento_codigo` STRING,
  `departamento_nombre` STRING,
  `provincia_codigo` STRING,
  `provincia_nombre` STRING,
  `distrito_codigo` STRING,
  `distrito_nombre` STRING,
  `ubigeo6_ejecutora` STRING,
  `fuente_financiamiento_codigo` STRING,
  `fuente_financiamiento_nombre` STRING,
  `rubro_codigo` STRING,
  `rubro_nombre` STRING,
  `tipo_recurso_codigo` STRING,
  `tipo_recurso_nombre` STRING,
  `generica_codigo` STRING,
  `generica_nombre` STRING,
  `subgenerica_codigo` STRING,
  `subgenerica_nombre` STRING,
  `subgenerica_det_codigo` STRING,
  `subgenerica_det_nombre` STRING,
  `especifica_codigo` STRING,
  `especifica_nombre` STRING,
  `especifica_det_codigo` STRING,
  `especifica_det_nombre` STRING,
  `monto_pia` DECIMAL(20,4),
  `monto_pim` DECIMAL(20,4),
  `monto_recaudado` DECIMAL(20,4),
  `is_municipal_government` BOOLEAN,
  `is_valid_anio` BOOLEAN,
  `is_valid_mes` BOOLEAN,
  `is_valid_sec_ejec` BOOLEAN,
  `is_valid_ubigeo6_ejecutora` BOOLEAN,
  `is_valid_monto_pia` BOOLEAN,
  `is_valid_monto_pim` BOOLEAN,
  `is_valid_monto_recaudado` BOOLEAN,
  `flag_pim_menor_pia` BOOLEAN,
  `flag_recaudado_mayor_pim` BOOLEAN,
  `has_complete_territory` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/siaf_income/resource_key=annual_2020';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`siaf_income__annual_2021` (
  `anio` INT,
  `mes` INT,
  `fecha_mes` DATE,
  `source_resource_key` STRING,
  `source_granularity` STRING,
  `nivel_gobierno_codigo` STRING,
  `nivel_gobierno_nombre` STRING,
  `sector_codigo` STRING,
  `sector_nombre` STRING,
  `pliego_codigo` STRING,
  `pliego_nombre` STRING,
  `sec_ejec` STRING,
  `ejecutora_codigo` STRING,
  `ejecutora_nombre` STRING,
  `departamento_codigo` STRING,
  `departamento_nombre` STRING,
  `provincia_codigo` STRING,
  `provincia_nombre` STRING,
  `distrito_codigo` STRING,
  `distrito_nombre` STRING,
  `ubigeo6_ejecutora` STRING,
  `fuente_financiamiento_codigo` STRING,
  `fuente_financiamiento_nombre` STRING,
  `rubro_codigo` STRING,
  `rubro_nombre` STRING,
  `tipo_recurso_codigo` STRING,
  `tipo_recurso_nombre` STRING,
  `generica_codigo` STRING,
  `generica_nombre` STRING,
  `subgenerica_codigo` STRING,
  `subgenerica_nombre` STRING,
  `subgenerica_det_codigo` STRING,
  `subgenerica_det_nombre` STRING,
  `especifica_codigo` STRING,
  `especifica_nombre` STRING,
  `especifica_det_codigo` STRING,
  `especifica_det_nombre` STRING,
  `monto_pia` DECIMAL(20,4),
  `monto_pim` DECIMAL(20,4),
  `monto_recaudado` DECIMAL(20,4),
  `is_municipal_government` BOOLEAN,
  `is_valid_anio` BOOLEAN,
  `is_valid_mes` BOOLEAN,
  `is_valid_sec_ejec` BOOLEAN,
  `is_valid_ubigeo6_ejecutora` BOOLEAN,
  `is_valid_monto_pia` BOOLEAN,
  `is_valid_monto_pim` BOOLEAN,
  `is_valid_monto_recaudado` BOOLEAN,
  `flag_pim_menor_pia` BOOLEAN,
  `flag_recaudado_mayor_pim` BOOLEAN,
  `has_complete_territory` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/siaf_income/resource_key=annual_2021';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`siaf_income__annual_2022` (
  `anio` INT,
  `mes` INT,
  `fecha_mes` DATE,
  `source_resource_key` STRING,
  `source_granularity` STRING,
  `nivel_gobierno_codigo` STRING,
  `nivel_gobierno_nombre` STRING,
  `sector_codigo` STRING,
  `sector_nombre` STRING,
  `pliego_codigo` STRING,
  `pliego_nombre` STRING,
  `sec_ejec` STRING,
  `ejecutora_codigo` STRING,
  `ejecutora_nombre` STRING,
  `departamento_codigo` STRING,
  `departamento_nombre` STRING,
  `provincia_codigo` STRING,
  `provincia_nombre` STRING,
  `distrito_codigo` STRING,
  `distrito_nombre` STRING,
  `ubigeo6_ejecutora` STRING,
  `fuente_financiamiento_codigo` STRING,
  `fuente_financiamiento_nombre` STRING,
  `rubro_codigo` STRING,
  `rubro_nombre` STRING,
  `tipo_recurso_codigo` STRING,
  `tipo_recurso_nombre` STRING,
  `generica_codigo` STRING,
  `generica_nombre` STRING,
  `subgenerica_codigo` STRING,
  `subgenerica_nombre` STRING,
  `subgenerica_det_codigo` STRING,
  `subgenerica_det_nombre` STRING,
  `especifica_codigo` STRING,
  `especifica_nombre` STRING,
  `especifica_det_codigo` STRING,
  `especifica_det_nombre` STRING,
  `monto_pia` DECIMAL(20,4),
  `monto_pim` DECIMAL(20,4),
  `monto_recaudado` DECIMAL(20,4),
  `is_municipal_government` BOOLEAN,
  `is_valid_anio` BOOLEAN,
  `is_valid_mes` BOOLEAN,
  `is_valid_sec_ejec` BOOLEAN,
  `is_valid_ubigeo6_ejecutora` BOOLEAN,
  `is_valid_monto_pia` BOOLEAN,
  `is_valid_monto_pim` BOOLEAN,
  `is_valid_monto_recaudado` BOOLEAN,
  `flag_pim_menor_pia` BOOLEAN,
  `flag_recaudado_mayor_pim` BOOLEAN,
  `has_complete_territory` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/siaf_income/resource_key=annual_2022';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`siaf_income__annual_2023` (
  `anio` INT,
  `mes` INT,
  `fecha_mes` DATE,
  `source_resource_key` STRING,
  `source_granularity` STRING,
  `nivel_gobierno_codigo` STRING,
  `nivel_gobierno_nombre` STRING,
  `sector_codigo` STRING,
  `sector_nombre` STRING,
  `pliego_codigo` STRING,
  `pliego_nombre` STRING,
  `sec_ejec` STRING,
  `ejecutora_codigo` STRING,
  `ejecutora_nombre` STRING,
  `departamento_codigo` STRING,
  `departamento_nombre` STRING,
  `provincia_codigo` STRING,
  `provincia_nombre` STRING,
  `distrito_codigo` STRING,
  `distrito_nombre` STRING,
  `ubigeo6_ejecutora` STRING,
  `fuente_financiamiento_codigo` STRING,
  `fuente_financiamiento_nombre` STRING,
  `rubro_codigo` STRING,
  `rubro_nombre` STRING,
  `tipo_recurso_codigo` STRING,
  `tipo_recurso_nombre` STRING,
  `generica_codigo` STRING,
  `generica_nombre` STRING,
  `subgenerica_codigo` STRING,
  `subgenerica_nombre` STRING,
  `subgenerica_det_codigo` STRING,
  `subgenerica_det_nombre` STRING,
  `especifica_codigo` STRING,
  `especifica_nombre` STRING,
  `especifica_det_codigo` STRING,
  `especifica_det_nombre` STRING,
  `monto_pia` DECIMAL(20,4),
  `monto_pim` DECIMAL(20,4),
  `monto_recaudado` DECIMAL(20,4),
  `is_municipal_government` BOOLEAN,
  `is_valid_anio` BOOLEAN,
  `is_valid_mes` BOOLEAN,
  `is_valid_sec_ejec` BOOLEAN,
  `is_valid_ubigeo6_ejecutora` BOOLEAN,
  `is_valid_monto_pia` BOOLEAN,
  `is_valid_monto_pim` BOOLEAN,
  `is_valid_monto_recaudado` BOOLEAN,
  `flag_pim_menor_pia` BOOLEAN,
  `flag_recaudado_mayor_pim` BOOLEAN,
  `has_complete_territory` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/siaf_income/resource_key=annual_2023';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`siaf_income__annual_2024` (
  `anio` INT,
  `mes` INT,
  `fecha_mes` DATE,
  `source_resource_key` STRING,
  `source_granularity` STRING,
  `nivel_gobierno_codigo` STRING,
  `nivel_gobierno_nombre` STRING,
  `sector_codigo` STRING,
  `sector_nombre` STRING,
  `pliego_codigo` STRING,
  `pliego_nombre` STRING,
  `sec_ejec` STRING,
  `ejecutora_codigo` STRING,
  `ejecutora_nombre` STRING,
  `departamento_codigo` STRING,
  `departamento_nombre` STRING,
  `provincia_codigo` STRING,
  `provincia_nombre` STRING,
  `distrito_codigo` STRING,
  `distrito_nombre` STRING,
  `ubigeo6_ejecutora` STRING,
  `fuente_financiamiento_codigo` STRING,
  `fuente_financiamiento_nombre` STRING,
  `rubro_codigo` STRING,
  `rubro_nombre` STRING,
  `tipo_recurso_codigo` STRING,
  `tipo_recurso_nombre` STRING,
  `generica_codigo` STRING,
  `generica_nombre` STRING,
  `subgenerica_codigo` STRING,
  `subgenerica_nombre` STRING,
  `subgenerica_det_codigo` STRING,
  `subgenerica_det_nombre` STRING,
  `especifica_codigo` STRING,
  `especifica_nombre` STRING,
  `especifica_det_codigo` STRING,
  `especifica_det_nombre` STRING,
  `monto_pia` DECIMAL(20,4),
  `monto_pim` DECIMAL(20,4),
  `monto_recaudado` DECIMAL(20,4),
  `is_municipal_government` BOOLEAN,
  `is_valid_anio` BOOLEAN,
  `is_valid_mes` BOOLEAN,
  `is_valid_sec_ejec` BOOLEAN,
  `is_valid_ubigeo6_ejecutora` BOOLEAN,
  `is_valid_monto_pia` BOOLEAN,
  `is_valid_monto_pim` BOOLEAN,
  `is_valid_monto_recaudado` BOOLEAN,
  `flag_pim_menor_pia` BOOLEAN,
  `flag_recaudado_mayor_pim` BOOLEAN,
  `has_complete_territory` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/siaf_income/resource_key=annual_2024';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`siaf_income__daily_2025` (
  `anio` INT,
  `mes` INT,
  `fecha_mes` DATE,
  `source_resource_key` STRING,
  `source_granularity` STRING,
  `nivel_gobierno_codigo` STRING,
  `nivel_gobierno_nombre` STRING,
  `sector_codigo` STRING,
  `sector_nombre` STRING,
  `pliego_codigo` STRING,
  `pliego_nombre` STRING,
  `sec_ejec` STRING,
  `ejecutora_codigo` STRING,
  `ejecutora_nombre` STRING,
  `departamento_codigo` STRING,
  `departamento_nombre` STRING,
  `provincia_codigo` STRING,
  `provincia_nombre` STRING,
  `distrito_codigo` STRING,
  `distrito_nombre` STRING,
  `ubigeo6_ejecutora` STRING,
  `fuente_financiamiento_codigo` STRING,
  `fuente_financiamiento_nombre` STRING,
  `rubro_codigo` STRING,
  `rubro_nombre` STRING,
  `tipo_recurso_codigo` STRING,
  `tipo_recurso_nombre` STRING,
  `generica_codigo` STRING,
  `generica_nombre` STRING,
  `subgenerica_codigo` STRING,
  `subgenerica_nombre` STRING,
  `subgenerica_det_codigo` STRING,
  `subgenerica_det_nombre` STRING,
  `especifica_codigo` STRING,
  `especifica_nombre` STRING,
  `especifica_det_codigo` STRING,
  `especifica_det_nombre` STRING,
  `monto_pia` DECIMAL(20,4),
  `monto_pim` DECIMAL(20,4),
  `monto_recaudado` DECIMAL(20,4),
  `is_municipal_government` BOOLEAN,
  `is_valid_anio` BOOLEAN,
  `is_valid_mes` BOOLEAN,
  `is_valid_sec_ejec` BOOLEAN,
  `is_valid_ubigeo6_ejecutora` BOOLEAN,
  `is_valid_monto_pia` BOOLEAN,
  `is_valid_monto_pim` BOOLEAN,
  `is_valid_monto_recaudado` BOOLEAN,
  `flag_pim_menor_pia` BOOLEAN,
  `flag_recaudado_mayor_pim` BOOLEAN,
  `has_complete_territory` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/siaf_income/resource_key=daily_2025';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`siaf_income__daily_2026` (
  `anio` INT,
  `mes` INT,
  `fecha_mes` DATE,
  `source_resource_key` STRING,
  `source_granularity` STRING,
  `nivel_gobierno_codigo` STRING,
  `nivel_gobierno_nombre` STRING,
  `sector_codigo` STRING,
  `sector_nombre` STRING,
  `pliego_codigo` STRING,
  `pliego_nombre` STRING,
  `sec_ejec` STRING,
  `ejecutora_codigo` STRING,
  `ejecutora_nombre` STRING,
  `departamento_codigo` STRING,
  `departamento_nombre` STRING,
  `provincia_codigo` STRING,
  `provincia_nombre` STRING,
  `distrito_codigo` STRING,
  `distrito_nombre` STRING,
  `ubigeo6_ejecutora` STRING,
  `fuente_financiamiento_codigo` STRING,
  `fuente_financiamiento_nombre` STRING,
  `rubro_codigo` STRING,
  `rubro_nombre` STRING,
  `tipo_recurso_codigo` STRING,
  `tipo_recurso_nombre` STRING,
  `generica_codigo` STRING,
  `generica_nombre` STRING,
  `subgenerica_codigo` STRING,
  `subgenerica_nombre` STRING,
  `subgenerica_det_codigo` STRING,
  `subgenerica_det_nombre` STRING,
  `especifica_codigo` STRING,
  `especifica_nombre` STRING,
  `especifica_det_codigo` STRING,
  `especifica_det_nombre` STRING,
  `monto_pia` DECIMAL(20,4),
  `monto_pim` DECIMAL(20,4),
  `monto_recaudado` DECIMAL(20,4),
  `is_municipal_government` BOOLEAN,
  `is_valid_anio` BOOLEAN,
  `is_valid_mes` BOOLEAN,
  `is_valid_sec_ejec` BOOLEAN,
  `is_valid_ubigeo6_ejecutora` BOOLEAN,
  `is_valid_monto_pia` BOOLEAN,
  `is_valid_monto_pim` BOOLEAN,
  `is_valid_monto_recaudado` BOOLEAN,
  `flag_pim_menor_pia` BOOLEAN,
  `flag_recaudado_mayor_pim` BOOLEAN,
  `has_complete_territory` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/siaf_income/resource_key=daily_2026';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`siaf_income__monthly_2025` (
  `anio` INT,
  `mes` INT,
  `fecha_mes` DATE,
  `source_resource_key` STRING,
  `source_granularity` STRING,
  `nivel_gobierno_codigo` STRING,
  `nivel_gobierno_nombre` STRING,
  `sector_codigo` STRING,
  `sector_nombre` STRING,
  `pliego_codigo` STRING,
  `pliego_nombre` STRING,
  `sec_ejec` STRING,
  `ejecutora_codigo` STRING,
  `ejecutora_nombre` STRING,
  `departamento_codigo` STRING,
  `departamento_nombre` STRING,
  `provincia_codigo` STRING,
  `provincia_nombre` STRING,
  `distrito_codigo` STRING,
  `distrito_nombre` STRING,
  `ubigeo6_ejecutora` STRING,
  `fuente_financiamiento_codigo` STRING,
  `fuente_financiamiento_nombre` STRING,
  `rubro_codigo` STRING,
  `rubro_nombre` STRING,
  `tipo_recurso_codigo` STRING,
  `tipo_recurso_nombre` STRING,
  `generica_codigo` STRING,
  `generica_nombre` STRING,
  `subgenerica_codigo` STRING,
  `subgenerica_nombre` STRING,
  `subgenerica_det_codigo` STRING,
  `subgenerica_det_nombre` STRING,
  `especifica_codigo` STRING,
  `especifica_nombre` STRING,
  `especifica_det_codigo` STRING,
  `especifica_det_nombre` STRING,
  `monto_pia` DECIMAL(20,4),
  `monto_pim` DECIMAL(20,4),
  `monto_recaudado` DECIMAL(20,4),
  `is_municipal_government` BOOLEAN,
  `is_valid_anio` BOOLEAN,
  `is_valid_mes` BOOLEAN,
  `is_valid_sec_ejec` BOOLEAN,
  `is_valid_ubigeo6_ejecutora` BOOLEAN,
  `is_valid_monto_pia` BOOLEAN,
  `is_valid_monto_pim` BOOLEAN,
  `is_valid_monto_recaudado` BOOLEAN,
  `flag_pim_menor_pia` BOOLEAN,
  `flag_recaudado_mayor_pim` BOOLEAN,
  `has_complete_territory` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/siaf_income/resource_key=monthly_2025';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`siaf_income__monthly_2026` (
  `anio` INT,
  `mes` INT,
  `fecha_mes` DATE,
  `source_resource_key` STRING,
  `source_granularity` STRING,
  `nivel_gobierno_codigo` STRING,
  `nivel_gobierno_nombre` STRING,
  `sector_codigo` STRING,
  `sector_nombre` STRING,
  `pliego_codigo` STRING,
  `pliego_nombre` STRING,
  `sec_ejec` STRING,
  `ejecutora_codigo` STRING,
  `ejecutora_nombre` STRING,
  `departamento_codigo` STRING,
  `departamento_nombre` STRING,
  `provincia_codigo` STRING,
  `provincia_nombre` STRING,
  `distrito_codigo` STRING,
  `distrito_nombre` STRING,
  `ubigeo6_ejecutora` STRING,
  `fuente_financiamiento_codigo` STRING,
  `fuente_financiamiento_nombre` STRING,
  `rubro_codigo` STRING,
  `rubro_nombre` STRING,
  `tipo_recurso_codigo` STRING,
  `tipo_recurso_nombre` STRING,
  `generica_codigo` STRING,
  `generica_nombre` STRING,
  `subgenerica_codigo` STRING,
  `subgenerica_nombre` STRING,
  `subgenerica_det_codigo` STRING,
  `subgenerica_det_nombre` STRING,
  `especifica_codigo` STRING,
  `especifica_nombre` STRING,
  `especifica_det_codigo` STRING,
  `especifica_det_nombre` STRING,
  `monto_pia` DECIMAL(20,4),
  `monto_pim` DECIMAL(20,4),
  `monto_recaudado` DECIMAL(20,4),
  `is_municipal_government` BOOLEAN,
  `is_valid_anio` BOOLEAN,
  `is_valid_mes` BOOLEAN,
  `is_valid_sec_ejec` BOOLEAN,
  `is_valid_ubigeo6_ejecutora` BOOLEAN,
  `is_valid_monto_pia` BOOLEAN,
  `is_valid_monto_pim` BOOLEAN,
  `is_valid_monto_recaudado` BOOLEAN,
  `flag_pim_menor_pia` BOOLEAN,
  `flag_recaudado_mayor_pim` BOOLEAN,
  `has_complete_territory` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/siaf_income/resource_key=monthly_2026';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`sismepre__ano_aplicacion` (
  `anio_aplicacion` INT,
  `anio_aplicacion_inicio` INT,
  `anio_aplicacion_fin` INT,
  `fecha_cierre` DATE,
  `estado` STRING,
  `periodo` INT,
  `fecha_pres_oficio` DATE,
  `fecha_ini_cierre` DATE,
  `fecha_ing` DATE,
  `is_active_record` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/sismepre/resource_key=ano_aplicacion';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`sismepre__entidad_estado` (
  `sec_ejec` STRING,
  `ubigeo6` STRING,
  `departamento_codigo` STRING,
  `departamento_nombre` STRING,
  `provincia_codigo` STRING,
  `provincia_nombre` STRING,
  `distrito_codigo` STRING,
  `distrito_nombre` STRING,
  `municipalidad_nombre` STRING,
  `anio_aplicacion` INT,
  `periodo` INT,
  `estado_registro` STRING,
  `is_active_record` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/sismepre/resource_key=entidad_estado';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`sismepre__esat_estadistica_atm` (
  `sec_ejec` STRING,
  `ubigeo6` STRING,
  `departamento_codigo` STRING,
  `departamento_nombre` STRING,
  `provincia_codigo` STRING,
  `provincia_nombre` STRING,
  `distrito_codigo` STRING,
  `distrito_nombre` STRING,
  `municipalidad_nombre` STRING,
  `anio_aplicacion` INT,
  `periodo` INT,
  `anio_estadistica` INT,
  `mes_estadistica` INT,
  `periodo_estadistica_tipo` STRING,
  `formulario_id` INT,
  `monto_emision_predial_afecto` DECIMAL(20,4),
  `monto_emision_predial_exonerado` DECIMAL(20,4),
  `monto_emision_predial_insoluto` DECIMAL(20,4),
  `monto_base_imponible_afecto` DECIMAL(20,4),
  `monto_base_imponible_exonerado` DECIMAL(20,4),
  `monto_autoavaluo_inafecto` DECIMAL(20,4),
  `numero_emision_predial_afecto` INT,
  `numero_emision_predial_exonerado` INT,
  `numero_emision_predial_casa_habitacion` INT,
  `numero_emision_predial_otros` INT,
  `monto_recaudacion_actual_ordinaria` DECIMAL(20,4),
  `monto_recaudacion_actual_coactiva` DECIMAL(20,4),
  `monto_recaudacion_anterior_ordinaria` DECIMAL(20,4),
  `monto_recaudacion_anterior_coactiva` DECIMAL(20,4),
  `monto_saldo_predial_ordinario` DECIMAL(20,4),
  `monto_saldo_predial_coactivo` DECIMAL(20,4),
  `numero_inafectos` INT,
  `numero_contribuyentes_predio` INT,
  `numero_predios_uso_casa_habitacion` INT,
  `numero_predios_otro_uso` INT,
  `numero_predios_total` INT,
  `monto_inicial_adulto_mayor` DECIMAL(20,4),
  `monto_predial_adulto_mayor` DECIMAL(20,4),
  `numero_contribuyentes_adulto_mayor` INT,
  `monto_recaudacion_adulto_mayor` DECIMAL(20,4),
  `tipo_meta` STRING,
  `flag_emision_liquidacion` STRING,
  `flag_emision_inicial` STRING,
  `monto_emision_predial_total` DECIMAL(22,4),
  `monto_recaudacion_predial_total` DECIMAL(24,4),
  `monto_saldo_predial_total` DECIMAL(22,4),
  `ratio_recaudacion_emision` DECIMAL(20,8),
  `is_valid_sec_ejec` BOOLEAN,
  `is_valid_ubigeo6` BOOLEAN,
  `is_valid_anio_aplicacion` BOOLEAN,
  `is_valid_periodo` BOOLEAN,
  `is_valid_anio_estadistica` BOOLEAN,
  `is_valid_mes_estadistica` BOOLEAN,
  `is_annual_stat_period` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/sismepre/resource_key=esat_estadistica_atm';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`sismepre__estadistica` (
  `anio_aplicacion` INT,
  `periodo` INT,
  `formulario_id` INT,
  `anio_estadistica` INT,
  `mes_estadistica` INT,
  `periodo_estadistica_tipo` STRING,
  `estado_registro` STRING,
  `anio_estadistica_desc` STRING,
  `is_active_record` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/sismepre/resource_key=estadistica';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`sismepre__formulario` (
  `anio_aplicacion` INT,
  `periodo` INT,
  `formulario_id` INT,
  `orden_formulario` STRING,
  `titulo` STRING,
  `sub_titulo` STRING,
  `abreviatura` STRING,
  `clasificacion` STRING,
  `tipo_formulario` STRING,
  `estado_registro` STRING,
  `is_active_record` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/sismepre/resource_key=formulario';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`sismepre__preguntas` (
  `anio_aplicacion` INT,
  `periodo` INT,
  `formulario_id` INT,
  `pregunta_id` INT,
  `pregunta_padre_id` INT,
  `orden_pregunta` STRING,
  `descripcion` STRING,
  `objeto_activo` STRING,
  `tipo_cuestionario_id` INT,
  `respuesta` STRING,
  `rango_ini` DECIMAL(20,4),
  `rango_fin` DECIMAL(20,4),
  `texto_apoyo` STRING,
  `texto_lectura` STRING,
  `estado_registro` STRING,
  `is_active_record` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/sismepre/resource_key=preguntas';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`sismepre__respuestas` (
  `sec_ejec` STRING,
  `anio_aplicacion` INT,
  `periodo` INT,
  `formulario_id` INT,
  `pregunta_id` INT,
  `respuesta_id` INT,
  `respuesta_texto` STRING,
  `respuesta_decimal` DECIMAL(20,4),
  `respuesta_entero` INT,
  `respuesta_fecha` DATE,
  `estado_registro` STRING,
  `is_active_record` BOOLEAN,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/sismepre/resource_key=respuestas';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`integration_coverage` (
  `coverage_scope` STRING,
  `source_name` STRING,
  `metric_name` STRING,
  `metric_value` DOUBLE,
  `total_records` BIGINT,
  `matched_records` BIGINT,
  `unmatched_records` BIGINT,
  `match_rate` DOUBLE,
  `issue_count` BIGINT,
  `issue_rate` DOUBLE,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/integrated/integration_coverage';

CREATE EXTERNAL TABLE IF NOT EXISTS `silver`.`map_sec_ejec_ubigeo` (
  `sec_ejec` STRING,
  `ubigeo6` STRING,
  `municipality_key` STRING,
  `has_siaf_match` BOOLEAN,
  `has_sismepre_match` BOOLEAN,
  `has_renamu_match` BOOLEAN,
  `has_classification_match` BOOLEAN,
  `match_status` STRING,
  `confidence_level` STRING,
  `issue_reason` STRING,
  `silver_source_name` STRING,
  `silver_resource_key` STRING,
  `silver_processed_at_utc` STRING
)
STORED AS PARQUET
LOCATION '/app/data/silver/integrated/map_sec_ejec_ubigeo';
